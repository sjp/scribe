{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Layout",
  "description": "Everything scribe knows about the text in one image.",
  "type": "object",
  "properties": {
    "version": {
      "description": "The version of the model this document was written against.\n\nThe current one is `LAYOUT_VERSION`.",
      "type": "integer",
      "format": "uint32",
      "minimum": 0
    },
    "image": {
      "description": "The raster the coordinates in this layout refer to.",
      "$ref": "#/$defs/ImageInfo"
    },
    "lines": {
      "description": "The lines of text, in reading order.",
      "type": "array",
      "items": {
        "$ref": "#/$defs/Line"
      },
      "default": []
    },
    "image_data_uri": {
      "description": "The image the layout was read from, as a base64 `data:` URI, for a document that carries its own picture.",
      "type": "string"
    }
  },
  "additionalProperties": false,
  "required": [
    "version",
    "image"
  ],
  "$defs": {
    "ImageInfo": {
      "description": "The raster a layout describes.",
      "type": "object",
      "properties": {
        "width": {
          "description": "The width of the image in pixels.",
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "height": {
          "description": "The height of the image in pixels.",
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        }
      },
      "additionalProperties": false,
      "required": [
        "width",
        "height"
      ]
    },
    "Line": {
      "description": "A line of recognised text.",
      "type": "object",
      "properties": {
        "text": {
          "description": "The text of the line, ready to be used as-is by a renderer.",
          "type": "string"
        },
        "bbox": {
          "description": "The axis-aligned bounds of the line.",
          "$ref": "#/$defs/Rect"
        },
        "rotated_box": {
          "description": "The oriented bounds of the line.",
          "$ref": "#/$defs/RotatedBox"
        },
        "words": {
          "description": "The words of the line, in reading order.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/Word"
          },
          "default": []
        },
        "confidence": {
          "description": "How sure the recogniser is of this line, from 0 to 1, when it says.",
          "type": [
            "number",
            "null"
          ],
          "format": "float",
          "default": null
        }
      },
      "additionalProperties": false,
      "required": [
        "text",
        "bbox",
        "rotated_box"
      ]
    },
    "Rect": {
      "description": "An axis-aligned rectangle in image pixels, with the origin at the top left\nand y increasing downwards.",
      "type": "object",
      "properties": {
        "x": {
          "description": "The x coordinate of the left edge.",
          "type": "number",
          "format": "float"
        },
        "y": {
          "description": "The y coordinate of the top edge.",
          "type": "number",
          "format": "float"
        },
        "width": {
          "description": "The width, extending to the right.",
          "type": "number",
          "format": "float"
        },
        "height": {
          "description": "The height, extending downwards.",
          "type": "number",
          "format": "float"
        }
      },
      "additionalProperties": false,
      "required": [
        "x",
        "y",
        "width",
        "height"
      ]
    },
    "RotatedBox": {
      "description": "An oriented rectangle in image pixels: a box of `width` by `height` centred\non `(cx, cy)` and turned by `angle_deg`.\n\nThe angle is measured from the positive x axis to the box's width axis and\nis positive clockwise as seen on screen, which is what SVG's `rotate()`\ndoes in the same y-down coordinate system.",
      "type": "object",
      "properties": {
        "cx": {
          "description": "The x coordinate of the centre.",
          "type": "number",
          "format": "float"
        },
        "cy": {
          "description": "The y coordinate of the centre.",
          "type": "number",
          "format": "float"
        },
        "width": {
          "description": "The extent along the box's width axis.",
          "type": "number",
          "format": "float"
        },
        "height": {
          "description": "The extent along the box's height axis.",
          "type": "number",
          "format": "float"
        },
        "angle_deg": {
          "description": "The rotation in degrees, clockwise-positive, normalised to\n`(-180, 180]`.",
          "type": "number",
          "format": "float"
        }
      },
      "additionalProperties": false,
      "required": [
        "cx",
        "cy",
        "width",
        "height",
        "angle_deg"
      ]
    },
    "Word": {
      "description": "A word within a line.",
      "type": "object",
      "properties": {
        "text": {
          "description": "The text of the word.",
          "type": "string"
        },
        "bbox": {
          "description": "The axis-aligned bounds of the word.",
          "$ref": "#/$defs/Rect"
        },
        "rotated_box": {
          "description": "The oriented bounds of the word.",
          "$ref": "#/$defs/RotatedBox"
        },
        "chars": {
          "description": "The characters of the word, in reading order.\n\nRenderers position words by default; this is here for the ones that\nwant finer control, and may be empty when the recogniser offers\nnothing per character.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/Char"
          },
          "default": []
        },
        "confidence": {
          "description": "How sure the recogniser is of this word, from 0 to 1, when it says.",
          "type": [
            "number",
            "null"
          ],
          "format": "float",
          "default": null
        }
      },
      "additionalProperties": false,
      "required": [
        "text",
        "bbox",
        "rotated_box"
      ]
    },
    "Char": {
      "description": "A single character within a word.",
      "type": "object",
      "properties": {
        "text": {
          "description": "The character itself, as one grapheme cluster.",
          "type": "string"
        },
        "bbox": {
          "description": "The axis-aligned bounds of the character.",
          "$ref": "#/$defs/Rect"
        },
        "confidence": {
          "description": "How sure the recogniser is of this character, from 0 to 1, when it\nsays.",
          "type": [
            "number",
            "null"
          ],
          "format": "float",
          "default": null
        }
      },
      "additionalProperties": false,
      "required": [
        "text",
        "bbox"
      ]
    }
  }
}
